<% if @student.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@student.errors.count, "error") %> prohibited this student from being saved:</h2>

  <ul>
  <% @student.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>
<div class="field">
	<%= f.label :name %>
	<%= f.text_field :name, :title => "Name" %>
</div>
<div class="field">
	<%= f.label :phone_number %>
	<%= f.text_field :phone_number, :title => "Mobile phone" %>
	<span class="field_description">SMS messages will be sent to this number if entered (optional)</span>
</div>
<div class="field">
	  <%= f.label :email %>
	  <%= f.text_field :email, :title => "Email" %>
	<span class="field_description">Emails will be sent to this address if entered (optional)</span>
</div>
<% #TODO: groups/memberships%>
<%= link_to 'Cancel', group_path(@group), :class => "button secondary minor margin_top" %>
<%= f.submit "Save", :class => 'button minor margin_top' %>