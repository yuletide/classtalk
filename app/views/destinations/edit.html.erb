<h1>Editing destination</h1>

<%= form_for([@group,@destination]) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Show', group_destination_path(@group,@destination) %> |
<%= link_to 'Back', group_destinations_path(@group) %>
