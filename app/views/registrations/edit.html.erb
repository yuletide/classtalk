<div class="left">
  <h2>Edit Account</h2>

  <%= form_for(resource, :as => resource_name, :url => profile_path, :html => { :method => :put }) do |f| %>
    <%= devise_error_messages! %>

    <fieldset>
      <div class="field">
        <%= f.label :first_name %>
        <%= f.text_field :first_name %>
      </div>
      <div class="field">
        <%= f.label :last_name %>
        <%= f.text_field :last_name %>
      </div>
      <div class="field">
        <%= f.label :display_name %>
        <%= f.text_field :display_name %>
      </div>
      <div class="field">
        <%= f.label :email %>
        <%= f.text_field :email %>
      </div>
      <div class="field">
        <%= f.label :phone_number %>
        <%= f.text_field :phone_number %>
      </div>
      <div class="field">
        <%= f.label :current_password %><%= link_to "Change?", edit_password_of_profile_path, :class => "right" %><br>
        <%= f.password_field :current_password %>
      </div>
    </fieldset>
    <%= link_to "Cancel", groups_path, :class => "button secondary left" %>
    <p class="right"><%= f.submit "Save" %></p>
</div>
<% end %>
